{
  "name": "gaia-meditasyon",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "npm run dev --prefix meditasyon/backend",
    "dev:web": "npx --yes serve meditasyon --listen 8080"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
